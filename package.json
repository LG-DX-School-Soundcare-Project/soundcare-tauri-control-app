{
  "name": "soundcare-tauri-control-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SoundCare Tauri ThinQ-style control app and web UI scaffold",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "dev:demo": "vite --host 0.0.0.0 --mode demo",
    "build": "vite build",
    "build:demo": "vite build --mode demo",
    "preview": "vite preview",
    "preview:demo": "vite preview --mode demo",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:dev:demo": "tauri dev --config src-tauri/tauri.demo.conf.json",
    "tauri:build": "tauri build",
    "tauri:build:demo": "tauri build --config src-tauri/tauri.demo.conf.json",
    "test": "vitest run",
    "check:syntax": "node scripts/check-js-syntax.mjs",
    "perf": "node scripts/perf/run-all.mjs",
    "perf:bundle": "node scripts/perf/measure-bundle.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "ffmpeg-static": "^5.3.0",
    "puppeteer": "^25.1.0",
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
